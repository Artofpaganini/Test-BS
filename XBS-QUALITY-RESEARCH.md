

# СТАТУС РЕФАКТОРА (живой)

- **P0 baseline** — DONE (сборка зелёная, снят экран-меню 19 кейсов).
- **P1 перф-ядро + 3 апрувнутых поведенческих фикса** — DONE (2026-07-21). Применено:
  - #1 additionalTopFraction: `animateFloatAsState` → `Animatable` + `snapshotFlow(additionalTopState)` (чтение `.value` в measure-фазе AdditionalTopMeasurePolicy).
  - #2 AdditionalTopMeasurePolicy — ОДИН инстанс (`remember {}` без ключей), `visibleFraction.value`/`overlapPx` читаются внутри measure.
  - #5 слот-лямбды detect/place вынесены в композицию SheetContainer (`detectBody`/`placeBody`) → `contentChanged=false` на layout-пассах.
  - C: `interactionsEnabled` → `derivedStateOf`.
  - Demo (XbsDemoScreen.kt:591) `contentAlpha` карточки: `animateFloatAsState` → `Animatable`.
  - E1 (апрув) AdditionalTop fills-баг: detect-пасс меряет surface loose → короткий additionalTop открывается Content-по-контенту, не peek. Регресс (g) ДО=`baseline/p1_g_exp.png` / ПОСЛЕ=`baseline/p1e_g_after.png`.
  - E2 (апрув) `onContentRemeasured`: условие overflow `contentHeightPx>maxHeightPx` → `measured.fills`.
  - E3 (апрув) `contentReady()`: гарды `isVisible` (закрытие в Loading не реоткрывает). Регресс (d) норм=`baseline/p1e_d_normal.png`.
- **P1 добор (решение team-lead: option-2, RULE #0 главнее перфа)** — DONE (2026-07-21):
  - A: `StayUnderKeyboardMeasurePolicy(keyboardState, navBarPx)` — IME читается ВНУТРИ measure (`remember(keyboardState, navBarPx)` = один инстанс). Регресс (l): bottom уходит ПОД клавиатуру и возвращается над nav bar, intrinsic-краша нет.
  - B: `SheetNestedScrollConnection(state, keyboardState, dragHandleVisible)` — доступность в колбэках (`remember(state, keyboardState, dragHandle)` = один инстанс). ПОМЕТКА: rule-3/стиль, не перф.
  - Lift-ветка (`liftBottomInset`, keyboardHeightPx) ОСТАЁТСЯ композиционной — под option-2 это принятая цена (см. факт ниже).
- **ФАКТ по частоте IME (замер logcat, кейсы i/l):** OnGlobalLayoutListener + `SOFT_INPUT_ADJUST_NOTHING` + value-dedup (`if (keyboardLift.value != newState)`) → **РОВНО 1 дискретное событие на открытие + 1 на закрытие** (не покадрово). Линза 1 («дискретные события») ПОДТВЕРЖДЕНА; моя ранняя формулировка «каждый кадр» — ОШИБОЧНА. Вывод: цена option-2 = ~1 рекомпозиция SheetBody на show + 1 на hide → option-1 (measure-фазная Lift-ветка) НЕ окупает поведенческий риск.
- **A/B ужаты до чистого rule-3 (решение team-lead) + принцип «фиксить осознанно»** — DONE (2026-07-21):
  - A: `remember { StayUnderKeyboardMeasurePolicy(keyboardState, navBarState) }` — БЕЗ ключей; navBar через `rememberUpdatedState` → State, читается в measure. (navBar-benefit нулевой — меняется лишь на ротацию; сделано как чистый идиом, не костыль.)
  - B: `remember(state) { SheetNestedScrollConnection(state, interactionsEnabledState) }` — enabled через ТОТ ЖЕ derivedStateOf (единый источник, что и sheetDrag), читается в колбэках. Пометка: rule-3/стиль, не перф.
- **P2 (все 4 пункта, апрув юзера попунктно)** — DONE (2026-07-21):
  - П1 кэш floor-якоря: `floorAnchorPx` считается в `updateMetrics` при смене метрик (прямой min-перебор, без list/Pair/distinctBy/sortedBy); `dragBy` читает поле. Регресс (j): свайп вниз не закрывает.
  - П2 `drop(1)` Hidden-коллектора: `snapshotFlow { state.isVisible }.drop(1).collect { if (!visible) onSheetHidden() }` — смонтированный скрытый лист не роняет чужой фокус. Регресс: скрим-тап закрывает.
  - П3 onPreFling гейт только по `isDragging` (убран `interactionsEnabled`): начатый драг доводит settle даже при смене доступности. Регресс: (b1) expand/collapse, (b2) → FullScreen, (a) свайп-закрытие.
  - П4 Lift-ветка IME-паддинга → measure-фаза: `keyboardHeightPx`/`imeVisible` УБРАНЫ из композиции корня; нижний inset Lift-ветки — `Modifier.layout { keyboardState.value ... }`. Регресс ДО/ПОСЛЕ (i) подъём+shrink, (l) bottom под клавой, (s) bottom над клавой — все 1:1.
- **ФАКТ IME после П4 (logcat, i/l/s):** по-прежнему РОВНО 1 дискретное событие на open + 1 на hide (не покадрово). keyboardHeightPx больше не живёт в композиции; корень реагирует на IME только булевой видимости (interactionsEnabled) — ~1 рекомпозиция/переход, высота полностью в layout-фазе.
- **P3 (нейминг/структура/удаления/комментарий)** — DONE (2026-07-21):
  - P3-А: 16 переименований (fills→isFillMode, contentReady→markContentReady, bodyOf→local fun sheetBodySlot, keyboardVisible→isKeyboardVisible, restore loading→isLoadingSaved, stayUnderKeyboard→isBottomUnderKeyboardMode, KeyboardBottomBehavior→BottomKeyboardBehavior, floorAnchorPx→lowestAllowedAnchorPx, rawOvershootPx→accumulatedOvershootPx, SlotDetect/Place→ContentMeasureSlot/VisibleContentSlot, AdditionalTopStack card/surface→additionalTopContent/sheetContent, SheetSnapshotFlowManager→ObserveSheetState [file mv], liftBottomInset→bottomInsetModifier, Preset1Button→PresetSingleButton [file mv], Dp-токены CELL_HEIGHT/MARKER_SIZE/BUTTON_HEIGHT→PascalCase, 0.5f→OVERSHOOT_ENTER_EPS_PX, ScrimFadeDistance→XBottomSheetDefaults). Осознанно-скип: imeVisible/hidden/lowestRestAnchorPx (удалены в П4/П2/П1).
  - P3-Б: (1) SheetContainer — SheetInsets + AdditionalTopConfig (data class, слоты отдельными параметрами, правило 10); (2) SheetBody → извлечены SheetSurface + LiftContent (зеркально StayUnderKeyboardContent, порядок модификаторов сохранён); (3) settleCandidates→buildSettleCandidates + data class AnchorCandidate(value, anchorPx).
  - P3-В: удалены мёртвые CollapsedFraction, AdditionalTopPeek; DEFAULT_PEEK_FRACTION→XBottomSheetDefaults.PeekFraction.
  - P3-Г: переписан врущий комментарий SheetDragGestures (resistance в ExpandedFullScreen тоже; убрана ссылка на несуществующий resolveDragTarget).
  - Смоук: (a) Content, (s) LiftContent FullScreen+IME (bottom над клавой), (g) additionalTop — все рендерятся 1:1. Доки обновлены на новые имена.
- **P4-DSL (оба DSL, БЕЗ require)** — DONE (2026-07-21):
  - Новый `XBottomSheetConfig.kt`: `@DslMarker XBottomSheetDsl`, `@Immutable data class XBottomSheetConfig(overlayBackground, dragHandle, additionalTopCornerRadius, dismiss: DismissConfig, keyboard: KeyboardConfig)` + companion Default; вложенные DismissConfig(onOutsideTap, onSwipeDown)/KeyboardConfig(bottomBehavior); билдеры var+build() (дефолты только в билдерах); `fun xBottomSheetConfig(configure = {})`. Лямбд в data class НЕТ (слоты — отдельные параметры). БЕЗ require.
  - Стейт-DSL в XBottomSheetState.kt: `@Immutable data class XBottomSheetStateConfig(skipCollapsed, initialLoading, peekFraction, anchors)`, билдер + `anchors { anchor(key, fraction) }`; конструктор XBottomSheetState(config); Saver захватывает config; `rememberXBottomSheetState { ... }`. Value-equality → стабильный ключ rememberSaveable.
  - Сигнатура: `XBottomSheet(state, onDismissRequest, modifier, config = XBottomSheetConfig.Default, additionalTop, top, bottom, middle)`. Старые скалярные параметры удалены.
  - Атомарная миграция demo: 4 стейт-DSL (r anchors, c/k skipCollapsed, d initialLoading) + 7 конфиг-листа (e overlay, f dragHandle=null, g cornerRadius, l/s keyboard, j dismiss, k dragHandle=Static). alva/zorderlab не используют — проверено. Коллизия имён `dismiss` (локальный val vs DSL-метод) резолвится корректно.
  - Верификация: assembleDebug зелёный; печать в (i) — лист НЕ пересоздаётся/не моргает (config value-equality → стабильный ключ, подтверждено сериями нажатий); (e) без затемнения + тень; (g) скруглённая карточка additionalTop. Доки XBOTTOMSHEET.md (API→DSL) + XBS-SPEC.md §3 (ПЕРЕСМОТРЕНО: DSL-конфиг) обновлены.
- **LOW-хвосты (8, финальный раунд, апрув юзера)** — DONE (2026-07-21):
  - L1 updateMetrics: сравнение 4 примитивов ДО аллокации SheetMetrics (горячий путь measure).
  - L2 AdditionalTop: убран лишний clipToBounds() (clip формы уже режет); shape оставлен инлайн ОСОЗНАННО (не покадровый путь + структурное равенство гасит инвалидацию; remember(cornerRadius){} = запрещённый rule-3 ради нуля).
  - L3 demo i/l/s: `filtered = remember(query) { … }` — легальный кэш вычисления (не создание сущности), поясн. комментом.
  - L4 markContentReady: фолбэк после таймаута НЕ принимает лоадерные метрики — ждёт первый инстанс после снятия isLoading (`awaitContentMetrics`, != loaderMetrics). Регресс (d) норм.
  - L5 Loading+IME: IME-коллектор ключуется парой `keyboardState.value to isLoading` → снятие Loading под открытой клавиатурой переоценивает промоушен (onImeShown идемпотентен).
  - L6 dragBy: потолок ОБЫЧНОГО драга = верхний rest-якорь (`highestAllowedAnchorPx`, кэш как lowest); выше — только overshoot с сопротивлением (драг за хендл из Collapsed больше не растягивает пустой Surface до потолка). Регресс (b1): held-драг вверх клампится на ExpandedContent без пустого стрейча.
  - L7 customAnchors: `Log.w` при создании стейта, если заданы якоря (без require — по юзеру). Проверено (r): варнинг в logcat.
  - L8 onPreFling: если offset уже на rest-якоре (составной жест: лист дорос + скролл списка) — не съедать скорость (`isOffsetAtRestAnchor()` → enqueueSettle(0), return Zero), инерция списка продолжается.
  - connection-LOW (XBottomSheet.kt:139) — уже закрыт в раунде A/B (single-instance remember(state) + enabledState).
  - Самопрогон: (r) L7 варнинг + открытие, (b1) L6 клампинг, (d) L4 норм, (i)/typing стабильность (P4-DSL). Сборка зелёная.
- **БАГ additionalTop (юзер): анимация карточки дёргала контент листа** — FIXED (2026-07-21):
  - Причина: (1) place-пасс делал `surface = offset − cardVisibleHeight` (surface сжимался/ехал вниз при росте карточки); (2) detect включал cardVisibleHeight → offset догонял фракцию ОТДЕЛЬНОЙ пружиной (коллектор роста), рассинхрон.
  - Фикс: карточка вынесена из тела листа в ОТДЕЛЬНЫЙ subcompose-слот `AdditionalTopCardSlot` (в корневом SubcomposeLayout). surface ВСЕГДА высотой offset (контент неподвижен); `totalHeight = offset + cardVisibleHeight`, fraction читается в measure того же пасса (deferred, синхронно, без пружины-догонялки); карточка вне detect → offset её не догоняет. AdditionalTopStack + AdditionalTopMeasurePolicy удалены (логика в SubcomposeLayout), остался enum AdditionalTopState. SheetBody больше не знает про additionalTop.
  - Приёмка (эмулятор): (1) кейс (g) тоггл — «Событие» на y=1650 в ОБОИХ состояниях (контент неподвижен), карточка «Добавить в купон» выезжает/уезжает НАД листом (скрины `baseline/bug_g_expanded.png` / `bug_g_collapsed.png`); (2) конечные высоты — Expanded=контент+карточка, Collapsed=контент (bottom-aligned, карточка сверху); (3) драг листа с карточкой работает (g свайп-вниз закрывает); (4) (a)/(b1) без additionalTop — путь идентичен старому (cardVisibleHeight=0 → totalHeight=offset), рендерятся 1:1.
- **ВСЯ ОЧЕРЕДЬ (P0→P1→A/B→P2→P3→P4-DSL→LOW-хвосты→additionalTop-баг) ЗАВЕРШЕНА.** Дальше — полная регрессия 19 кейсов + коммит на стороне team-lead.

---

# Линза 5 — эргономика API + DSL: дизайн конфигов rememberXBottomSheetState / XBottomSheet, план миграции 19 demo-кейсов, сравнение с M3 ModalBottomSheet и AlvaBottomSheetSceneConfigBuilder

## [HIGH] XBottomSheet.kt:63
PROBLEM: Сигнатура XBottomSheet — 13 параметров, из них 6 скалярных конфигов (overlayBackground, dragHandle, dismissOnOutsideTap, dismissOnSwipeDown, bottomKeyboardBehavior, additionalTopCornerRadius) вперемешку с callback'ом и слотами (строки 63-77). История раундов 4-7 показывает темп роста: bottomKeyboardBehavior добавлен раундом 5, additionalTopCornerRadius — раундом 4; каждый новый факт = +1 параметр в простыню и source-break всех вызовов. Это ровно тот анти-паттерн «простыня параметров», против которого предпочтение юзера DSL-first. Механика: параметрная простыня не масштабируется (при переносе в 1XUI Core добавятся ещё поведения — secure/back-press по образцу M3 ModalBottomSheetProperties), а группировка постфактум станет breaking change для внешних команд.
FIX: Новый файл XBottomSheetConfig.kt (дизайн, по образцу AlvaBottomSheetSceneConfigBuilder.kt:15-49, но с обязательной структурной equality): (1) @DslMarker annotation class XBottomSheetDsl; (2) @Immutable data class XBottomSheetConfig internal constructor(val overlayBackground: Boolean, val dragHandle: DragHandleStyle?, val additionalTopCornerRadius: Dp, val dismiss: DismissConfig, val keyboard: KeyboardConfig) { companion object { val Default = xBottomSheetConfig() } } — data class ОБЯЗАТЕЛЬНО: equality по значению даёт skip рекомпозиции при inline-создании конфига на call-site (remember вокруг не нужен — и не оборачивать, правило юзера); дефолты живут ТОЛЬКО в билдере (правило workspace: data class без дефолтов в конструкторе); лямбд внутри нет (правило «конфиг — чистые данные», как GestureCommand в XBottomSheetState.kt:153). (3) Вложенные группы: data class DismissConfig(val onOutsideTap: Boolean, val onSwipeDown: Boolean); data class KeyboardConfig(val bottomBehavior: KeyboardBottomBehavior). (4) @XBottomSheetDsl class XBottomSheetConfigBuilder { var overlayBackground = true; var dragHandle: DragHandleStyle? = DragHandleStyle.Theme; var additionalTopCornerRadius = 0.dp; private val dismissBuilder = DismissConfigBuilder(); private val keyboardBuilder = KeyboardConfigBuilder(); fun dismiss(block: DismissConfigBuilder.() -> Unit) = dismissBuilder.block(); fun keyboard(block: KeyboardConfigBuilder.() -> Unit) = keyboardBuilder.block(); fun build(): XBottomSheetConfig } — повторные вызовы dismiss{}/keyboard{} мёржатся last-write-wins. (5) top-level fun xBottomSheetConfig(configure: XBottomSheetConfigBuilder.() -> Unit = {}): XBottomSheetConfig = XBottomSheetConfigBuilder().apply(configure).build(). Структура плоская для визуальной тройки (overlayBackground/dragHandle/additionalTopCornerRadius — группировать нечего) + две вложенные группы там, где есть кластер и ожидание роста: dismiss{} (2 флага), keyboard{} (bottomBehavior, в будущем imeMode/secure). Итоговая сигнатура: XBottomSheet(state, onDismissRequest, modifier = Modifier, config: XBottomSheetConfig = XBottomSheetConfig.Default, additionalTop = null, top = null, bottom = null, middle) — residual-параметры (state, onDismissRequest, modifier, все 4 слота) остаются КАК ЕСТЬ. Внутри: SideEffect на строках 132-135 читает config.dismiss.onSwipeDown вместо параметра; interactionsEnabled (строка 104) — config.dragHandle. Пример call-site: XBottomSheet(state = state, onDismissRequest = dismiss, config = xBottomSheetConfig { overlayBackground = false; dismiss { onOutsideTap = false; onSwipeDown = false }; keyboard { bottomBehavior = KeyboardBottomBehavior.StayUnderKeyboard } }, top = {...}) {...}. Сравнение (задание г): M3 ModalBottomSheet — плоская простыня 12+ параметров (shape/цвета/elevation/insets), но и Google сгруппировал поведение в ModalBottomSheetProperties (securePolicy, shouldDismissOnBackPress) — тот же ход, что dismiss{}/keyboard{}; цветов у XBS в API нет по спеке §9, поэтому конфиг компактнее M3. Alva-образец — берём @DslMarker, var-дефолты в билдере, internal constructor у конфига, top-level фабрику с `= {}`; НЕ берём identity-equality (см. риск).
RISK: Три риска. (1) Если XBottomSheetConfig сделать обычным классом как AlvaBottomSheetSceneConfig (AlvaBottomSheetSceneConfig.kt:11 — @Stable без equals), inline-вызов xBottomSheetConfig{} на каждой рекомпозиции хоста создаст «новый» параметр → XBottomSheet перестанет скипаться (для Alva-сцены это неважно — она строится раз на nav-entry, для листа, рекомпозирующегося с экраном — важно). Проверка: кейс (i) — печать в поиске рекомпозирует хост; Layout Inspector / composition count у XBottomSheet не должен расти от каждого символа. (2) Провод config.dismiss.onSwipeDown через SideEffect (строки 132-135) — регрессия кейсов (j) свайп-pinned и (a) свайп-закрытие; прогнать оба. (3) Теневой конфликт имён на call-site: в demo локальный val dismiss: () -> Unit рядом с методом билдера dismiss{} — резолвится в пользу метода (invoke-конвенция для () -> Unit с лямбдой-аргументом не типизируется), но проверяется компиляцией; если всплывёт — переименовать локальные val в demo, не метод DSL.

## [MEDIUM] XBottomSheetState.kt:289
PROBLEM: rememberXBottomSheetState(skipCollapsed, initialLoading, peekFraction, customAnchors) — 4 конфиг-параметра простынёй, включая List<XSheetAnchor>: call-site кейса (r) выглядит как rememberXBottomSheetState(customAnchors = listOf(XSheetAnchor("half", 0.5f))) (XbsDemoScreen.kt:194) — двойная вложенность конструкторов вместо декларативного описания. Механика роста: peekFraction и customAnchors уже добавлены после спеки (§3 спеки давала только 2 параметра, XBS-SPEC.md:69-72) — каждый следующий факт стейт-машины снова расширит сигнатуру и список ключей rememberSaveable (строки 294-296, сейчас 4 позиционных ключа + Saver с теми же 4 аргументами — дублирование).
FIX: DSL-фабрика с тем же именем (нейминг простой, без экзотики): @Composable fun rememberXBottomSheetState(configure: XBottomSheetStateConfigBuilder.() -> Unit = {}): XBottomSheetState { val config = XBottomSheetStateConfigBuilder().apply(configure).build(); return rememberSaveable(config, saver = xBottomSheetStateSaver(config)) { XBottomSheetState(config) } }. Билдер: @XBottomSheetDsl class XBottomSheetStateConfigBuilder { var skipCollapsed = false; var initialLoading = false; var peekFraction = DEFAULT_PEEK_FRACTION; private val anchorList = mutableListOf<XSheetAnchor>(); fun anchors(block: XSheetAnchorsBuilder.() -> Unit); internal fun build(): XBottomSheetStateConfig } где @XBottomSheetDsl class XSheetAnchorsBuilder { fun anchor(key: String, heightFraction: Float) }. Продукт — @Immutable internal data class XBottomSheetStateConfig(skipCollapsed, initialLoading, peekFraction, anchors: List<XSheetAnchor>): data-equality решает главную механику — configure-лямбда исполняется КАЖДУЮ рекомпозицию, но равный по значению config не инвалидирует ключ rememberSaveable (тот же контракт, что сегодня у 4 позиционных ключей на строке 295). Конструктор XBottomSheetState принимает config; существующие чтения skipCollapsed (строки 65, 92, 100, 195), peekFraction/customAnchors (строки 145-146) делегируют в config — стейт-машина не меняется. Saver (строки 308-329) захватывает один config вместо 4 аргументов. Call-site кейса (r): rememberXBottomSheetState { anchors { anchor("half", 0.5f) } }. Семантика «конфиг только при создании; смена конфига пересоздаёт стейт» сохраняется 1:1 — задокументировать в KDoc-комментарии билдера. Альтернатива-упрощение (обсудить): плоский fun anchor(key, fraction) прямо на стейт-билдере без вложенного anchors{} — меньше скобок при одном якоре; вложенный вариант оставлен как основной по заданной форме (яснее группирует список). Сравнение (г): M3 rememberModalBottomSheetState(skipPartiallyExpanded, confirmValueChange) — плоская и с лямбдой confirmValueChange в стейте; XBS сознательно без лямбд в конфиге (контроль закрытия = dismiss-флаги + onDismissRequest) — правильнее по правилу проекта, не копировать M3.
RISK: Ключ rememberSaveable меняется с 4 значений на 1 config-объект: если config не data class — ключ «новый» каждую рекомпозицию → стейт листа сбрасывается непрерывно (лист «моргает» в Hidden). Проверка: кейс (i) — печать в поле поиска (рекомпозиция хоста) не должна сбрасывать лист; ротация на открытых (c)/(d)/(r) — стейт восстановлен (Saver-путь). Второй риск: нельзя оставлять старую сигнатуру рядом с новой — rememberXBottomSheetState() станет ambiguity (обе применимы без аргументов); переход только атомарный, см. план миграции.

## [LOW] SheetAnchors.kt:23
PROBLEM: Конфиг-входы не валидируются: XSheetAnchor(key, heightFraction) принимает любую fraction (в т.ч. 1.5f / -0.2f) и пустой/дублирующийся key; peekFraction в rememberXBottomSheetState — аналогично. Ошибки маскируются молча: customAnchorPx берёт firstOrNull и при отсутствии ключа тихо подставляет peekFraction (SheetAnchors.kt:46), coerceIn прячет выход за диапазон (SheetAnchors.kt:40, 47), дубликат ключа тихо схлопывается distinctBy по px в settleCandidates (XBottomSheetState.kt:269). Разработчик получает «не тот якорь» без единого сигнала.
FIX: Валидация — в build() DSL-билдеров, ровно по предпочтению юзера «обязательность/корректность — runtime require, не compile-time строгость»: require(peekFraction in 0f..1f) { "peekFraction $peekFraction вне 0..1" }; в XSheetAnchorsBuilder.anchor(): require(heightFraction > 0f && heightFraction < 1f) — строго между Hidden(0) и FullScreen(1); require(key.isNotBlank()); в build(): require(uniqueKeys) по списку. Диапазоны подобраны так, что все текущие значения demo (2/3, 0.5f, "half") проходят. Существующие coerceIn в SheetMetrics не трогать (они защищают от рантайм-геометрии, не от конфига).
RISK: Require может уронить вызов, который раньше «работал» на молчаливой коэрции (в лабе таких нет — проверено по 19 кейсам: единственный кастомный якорь 0.5f валиден). Проверка: компиляция + кейс (r) свайп peek → 50% → full.

## [MEDIUM] XbsDemoScreen.kt:194
PROBLEM: Обратная совместимость (задание в): 19 demo-кейсов завязаны на текущие сигнатуры; при переводе на DSL ломаются ровно 11 call-sites — 4 стейтовых: (r) customAnchors XbsDemoScreen.kt:194, (c) skipCollapsed :357, (d) initialLoading :373, (k) skipCollapsed :558 — и 7 листовых: (e) overlayBackground=false :399, (f) dragHandle=null :417, (g) additionalTopCornerRadius=16.dp :437, (j) dismissOnOutsideTap/dismissOnSwipeDown=false :543-544, (k) dragHandle=Static :565, (l) StayUnderKeyboard :495, (s) явный Lift :521. Остальные 8 кейсов используют дефолты и не меняются вовсе (дефолтная configure-лямбда `= {}` и config = Default).
FIX: План миграции в 4 фазы. Фаза 1 (аддитивная, компиляция зелёная): добавить XBottomSheetConfig.kt (конфиг листа + билдеры + @DslMarker + фабрика) и XBottomSheetStateConfig (в XBottomSheetState.kt); XBottomSheetState получает config-конструктор; старый API не тронут. Фаза 2 (атомарный switch, один коммит — API internal, single-module, внешних потребителей нет, deprecation-цикл не нужен и невозможен из-за overload-ambiguity дефолтных форм): заменить сигнатуры XBottomSheet и rememberXBottomSheetState; компилятор перечислит все 11 точек; правки механические: rememberXBottomSheetState(skipCollapsed = true) → rememberXBottomSheetState { skipCollapsed = true }; rememberXBottomSheetState(initialLoading = true) → { initialLoading = true }; customAnchors = listOf(XSheetAnchor("half", 0.5f)) → { anchors { anchor("half", 0.5f) } }; overlayBackground = false → config = xBottomSheetConfig { overlayBackground = false }; dragHandle = null → config { dragHandle = null }; dismissOnOutsideTap = false, dismissOnSwipeDown = false → config { dismiss { onOutsideTap = false; onSwipeDown = false } }; bottomKeyboardBehavior = StayUnderKeyboard → config { keyboard { bottomBehavior = ... } }; в кейсе (s) явный Lift-дефолт сохранить как keyboard { bottomBehavior = Lift } — кейс демонстрирует контраст с (l), явность нужна. Фаза 3 (верификация): assembleDebug + ручной прогон затронутых кейсов (c, d, e, f, g, j, k, l, r, s) по матрице XBS-RESULTS + ротация открытого листа (стабильность ключа rememberSaveable) + печать в (i) (отсутствие сброса стейта от рекомпозиций). Фаза 4 (доки): обновить блок API в XBOTTOMSHEET.md:39-63 и §3 XBS-SPEC.md:48-73 с пометкой «ПЕРЕСМОТРЕНО (раунд 8): DSL-конфиг». Для будущего переноса в 1XUI Core: публиковать ТОЛЬКО DSL-форму (снять internal с XBottomSheet, rememberXBottomSheetState, xBottomSheetConfig, билдеров, DragHandleStyle, KeyboardBottomBehavior, XSheetAnchor); @Deprecated-мост со старой простынёй параметров не тащить — её никогда не было в публичном API.
RISK: Атомарный switch без моста: единственный риск — пропущенный call-site вне demo (грепнуть rememberXBottomSheetState и XBottomSheet( по модулю перед фазой 2; com.alva.* и com.zorderlab.* компонент не используют — проверено grep'ом структуры). Функциональные регрессии закрывает фаза 3 — прогон по существующей матрице XBS-RESULTS.md.

OK: Слоты additionalTop/top/bottom/middle как отдельные composable-параметры с middle trailing-лямбдой (XBottomSheet.kt:73-76) — правильная форма, зеркалит Figma-слоты спеки §3; в конфиг НЕ переносить (residual вместе с state, onDismissRequest, modifier) | Цвета/высоты сознательно НЕ в API (XBottomSheetDefaults.kt:18, запрет спеки §9) — благодаря этому конфиг остаётся компактным (6 полей против 12+ у M3 ModalBottomSheet); не расширять конфиг цветами при переносе в Core | Императивное suspend-API стейта show/expand/hide/contentReady + вычисляемый read-only currentValue (XBottomSheetState.kt:33-93) — канон M3 SheetState; DSL-ифицировать команды не нужно | Конфиг жестов уже хранится в стейте как чистые данные без лямбд, а onDismissRequest проводится через SideEffect + rememberUpdatedState (XBottomSheet.kt:81, 132-135; комментарий XBottomSheetState.kt:151-153) — новый DSL-конфиг наследует этот принцип без изменений | XSheetAnchor — уже @Immutable data class (SheetAnchors.kt:22-23), встраивается в anchors{}-DSL как есть, без новых типов данных | rememberSaveable уже ключуется конфиг-значениями и Saver отделяет сохраняемое (currentValue/isLoading/additionalTopState) от конфига разработчика (XBottomSheetState.kt:294-329) — DSL-конфиг подключается одним ключом без перестройки Saver-логики | Дефолты текущих параметров (Theme-хендл, оба dismiss=true, Lift, 0.dp, peek 2/3) переезжают в билдеры буквально — поведенческих изменений ноль | В репо уже есть домашний стиль DSL-билдера — AlvaBottom


# Линза 4 — Нейминг и кодстайл (com/onexui/bottomsheet + demo)

## [HIGH] AdditionalTop.kt:71
PROBLEM: protrusionPx — юзер прямо назвал имя непонятным («протрузия» — медицинский жаргон). Функция считает видимую часть карточки Additional Top НАД листом; слово встречается также в локале protrusion (:83), в комментариях (:69, :84, :95) и не самообъясняется без чтения формулы.
FIX: Переименовать: fun protrusionPx(...) → cardVisibleHeightPx(cardNaturalPx), локаль protrusion → cardVisibleHeight, surfaceHeight/place-логика не меняется. Комментарии переписать со слова «протрузия» на «видимая часть карточки над листом».
RISK: Нулевой — приватный метод MeasurePolicy, чистый rename. Проверка: компиляция + демо-кейс (g) Additional Top (Expanded/Collapsed анимация высоты).

## [HIGH] SheetAnchors.kt:43
PROBLEM: SheetMetrics.fills — булево свойство без is/has/can-префикса (правило workspace) и криптичное: «fills» — что заполняет? Это ключевой флаг выбора режима якорей (wrap/fill), читается в XBottomSheetState.settleCandidates (:251) и openTarget/expandTarget (:53, :61) — самое важное имя стейт-машины.
FIX: Переименовать в isFillMode (совпадает со словарём документации «fill-режим») либо contentFillsScreen. Механика: contentHeightPx >= maxHeightPx — имя isFillMode сразу связывает замер с режимом якорей.
RISK: Нулевой — internal property, чистый rename по 4 использованиям. Проверка: компиляция + кейсы (a) wrap и (b2)/(m) fill.

## [HIGH] XBottomSheetDefaults.kt:22
PROBLEM: Мёртвые и конфликтующие токены: CollapsedFraction = 0.60f (строка 22) и AdditionalTopPeek = 20.dp (строка 26) НЕ используются нигде (проверено grep — только определения). При этом реальный дефолт peek живёт в другом файле: DEFAULT_PEEK_FRACTION = 2f/3f (XBottomSheetState.kt:356). Два источника правды для одной величины: будущий разработчик «починит» peek через мёртвый 0.60f и ничего не изменится.
FIX: Удалить CollapsedFraction и AdditionalTopPeek (после подтверждения тимлидом — правило «не удалять без спроса»); DEFAULT_PEEK_FRACTION перенести в XBottomSheetDefaults как val PeekFraction = 2f/3f — все токены геометрии в одном объекте.
RISK: Удаление неиспользуемого — риска нет (grep подтвердил 0 использований). Перенос константы — rename import в rememberXBottomSheetState. Проверка: компиляция + кейс открытия Collapsed на 2/3 экрана.

## [MEDIUM] XBottomSheet.kt:213
PROBLEM: SheetContainer — параметр-простыня: 20 параметров (state, 4 int-метрики, 3 boolean/enum, connection, keyboardState, fraction, cornerRadius, 4 слота, modifier); SheetBody — 15. Правило workspace: RO-RO при взрыве параметров. Каждый новый параметр прокидывается через 3 уровня (XBottomSheet → SheetContainer → SheetBody) — шум в диффах и лёгкость перепутать местами однотипные Int.
FIX: Сгруппировать скаляры в конфиг-объекты: data class SheetInsets(statusBarPx, navBarPx, keyboardHeightPx), data class AdditionalTopConfig(fraction, cornerRadius). Слоты-composable оставить явными параметрами (правило юзера: лямбды не поля data class). Итог ~12 параметров у SheetContainer.
RISK: Data class с примитивами стабилен для Compose (equals-based skip сохраняется); но новый инстанс на каждый кадр анимации additionalTopFraction — держать fraction ОТДЕЛЬНЫМ параметром, в конфиг не включать (иначе лишние сравнения). Проверка: демо-кейсы g/i/l/s (IME + AdditionalTop) визуально без изменений.

## [MEDIUM] XBottomSheet.kt:305
PROBLEM: SheetBody — 114 строк (305–418) и смешение уровней абстракции: сборка модификаторов + ДВА полных лейаута-ветки (StayUnderKeyboard-Column и Lift-Column с инсет-логикой) + lambda-локали middleContent/sheetSurface + оверлей DragHandle. Ветка StayUnderKeyboard уже вынесена в StayUnderKeyboardContent, а симметричная Lift-ветка (:369–391) — нет.
FIX: Извлечь Lift-ветку в private @Composable LiftContent(...) зеркально StayUnderKeyboardContent; sheetSurface-лямбду — в private @Composable SheetSurface(...). SheetBody останется диспетчером ~40 строк с одним уровнем абстракции.
RISK: Риск — случайно потерять порядок модификаторов (liftBottomInset/weight/middleShrinkModifier) при извлечении. Проверка: кейсы (a) bottom над nav bar, (i) FullScreen+shrink, (s) bottom над клавиатурой, (l) bottom под клавиатурой.

## [MEDIUM] SheetDragGestures.kt:10
PROBLEM: Устаревший и ФАКТИЧЕСКИ НЕВЕРНЫЙ комментарий: ссылается на несуществующий XBottomSheetState.resolveDragTarget (grep — метода нет, логика теперь в dragBy) и утверждает «В Collapsed и FullScreen НЕ применяется», хотя dragBy (XBottomSheetState.kt:197–204) применяет resistance и в ExpandedFullScreen (atTop → resistanceState, потолок maxHeight + RESISTANCE_MAX_PX). Комментарий вводит в заблуждение при сопровождении.
FIX: Переписать: «Применяется в Content, ExpandedContent и ExpandedFullScreen (см. XBottomSheetState.dragBy); в Collapsed не применяется». Убрать ссылку на resolveDragTarget.
RISK: Нулевой — правка комментария. Проверка не нужна.

## [MEDIUM] XBottomSheetState.kt:245
PROBLEM: settleCandidates возвращает List<Pair<SheetValue, Int>> — примитивная одержимость (правило workspace «no primitive obsession»): в settle читается chosen.first / candidate.second (:232–240) — в самой тонкой логике компонента (выбор якоря по velocity) имена полей ничего не говорят. Плюс имя функции не с глагола.
FIX: private data class AnchorCandidate(val value: SheetValue, val anchorPx: Int); функцию переименовать в buildSettleCandidates. candidate.second → candidate.anchorPx, chosen.first → chosen.value — логика settle станет читаемой.
RISK: Минимальный — механическая замена Pair на data class (distinctBy/sortedBy по anchorPx сохраняют поведение). Проверка: свайпы в кейсах (b1)/(b2)/(r): стоп на ближайшем якоре, fling через порог.

## [MEDIUM] XBottomSheet.kt:100
PROBLEM: Систематическое нарушение boolean-правила is/has/can: imeVisible (:100), keyboardVisible (StayUnderKeyboardMeasurePolicy :459), stayUnderKeyboard (:324 — читается как команда, а это флаг режима), hidden в лямбде SheetSnapshotFlowManager.kt:49, параметр loading в XBottomSheetState.restore (:70). При этом рядом isWide, isFullScreen, isLoading — стиль неоднороден внутри одного файла.
FIX: imeVisible → isImeVisible; keyboardVisible → isKeyboardVisible; stayUnderKeyboard → isBottomUnderKeyboardMode; hidden → isHidden; restore(loading) → restore(isLoadingSaved). interactionsEnabled допустимо оставить (суффикс Enabled — устоявшийся Compose-паттерн draggable(enabled=)).
RISK: Нулевой — чистые rename локалей/параметров. Проверка: компиляция.

## [MEDIUM] XBottomSheetState.kt:86
PROBLEM: contentReady() — публичный метод API не с глагола (правило workspace «функции с глагола»); ломает однородный ряд show()/expand()/hide(). Читается как свойство-вопрос, а это команда «контент готов — перемеряй и анимируй».
FIX: Переименовать в markContentReady() (или notifyContentReady()). Обновить вызов в демо (XbsDemoScreen.kt:378) и контракт в XBOTTOMSHEET.md/XBS-SPEC.md.
RISK: Это публичное API компонента — при переносе в 1XUI потянет за собой доки и вызовы. Проверка: компиляция + кейс (d) Loading → contentReady.

## [MEDIUM] XBottomSheet.kt:244
PROBLEM: bodyOf — имя-огрызок («тело чего?»), а вызовы bodyOf(false)/bodyOf(true) (:278, :288) — классический boolean trap: у лямбда-вызова нельзя написать именованный аргумент, читающий не знает, что значит true.
FIX: Заменить lambda-val на локальную @Composable fun sheetBodySlot(fillHeight: Boolean) — у локальной функции именованные аргументы работают: subcompose(SlotDetect) { sheetBodySlot(fillHeight = false) }. Имя + именованный вызов снимают обе проблемы.
RISK: Минимальный: локальная fun вместо val-лямбды в composable — семантика subcompose не меняется (тот же вызов SheetBody). Проверка: кейсы wrap (a) и fill (b2) — детект режима работает.

## [LOW] XBottomSheetState.kt:217
PROBLEM: lowestRestAnchorPx — «rest» это физический жаргон (положение покоя), не самообъясняется; фактически это самый нижний РАЗРЕШЁННЫЙ якорь, ниже которого драг не пускаем при dismissOnSwipeDown=false.
FIX: Переименовать в lowestAllowedAnchorPx (или minAllowedAnchorPx) — прямо описывает роль floor'а в dragBy (:196).
RISK: Нулевой — private метод. Проверка: кейс (j) — свайп вниз при выключенном закрытии не уводит лист ниже якоря.

## [LOW] XBottomSheet.kt:60
PROBLEM: Перевёрнутый порядок слов: тип KeyboardBottomBehavior (:60), а параметр — bottomKeyboardBehavior (:71). Одно и то же понятие в двух порядках — при поиске по коду половина вхождений теряется.
FIX: Привести к одному порядку: enum → BottomKeyboardBehavior (параметр уже так называется).
RISK: Нулевой — internal enum, rename + import в демо. Проверка: компиляция.

## [LOW] XBottomSheet.kt:374
PROBLEM: liftBottomInset — держит Modifier, но назван как значение инсета; ломает локальную конвенцию файла, где ВСЕ такие локали с суффиксом Modifier: widthModifier (:96), shadowModifier (:236), keyboardAdjustmentModifier (:237), sizeModifier (:327), middleShrinkModifier (:328).
FIX: Переименовать в bottomInsetModifier.
RISK: Нулевой — локальная переменная. Проверка: компиляция.

## [LOW] XBottomSheet.kt:53
PROBLEM: ScrimFadeDistance = 120.dp — имя хорошее, но токен живёт в шапке XBottomSheet.kt, тогда как ВСЕ остальные вшитые токены геометрии/цвета собраны в XBottomSheetDefaults (комментарий там прямо говорит «вшитые токены §2»). Разработчик, меняющий токены, этот не найдёт.
FIX: Перенести в XBottomSheetDefaults как val ScrimFadeDistance = 120.dp.
RISK: Нулевой — перенос private val в internal object. Проверка: компиляция + затемнение при открытии.

## [LOW] XBottomSheet.kt:297
PROBLEM: SlotDetect / SlotPlace — обратный порядок слов (читается «слот-детект» вместо «детект-слот»), и «Detect» не говорит, ЧТО детектится. Роли: невидимый замер натуральной высоты контента vs видимая композиция.
FIX: Переименовать: SlotDetect → ContentMeasureSlot, SlotPlace → VisibleContentSlot (или короче MeasureSlot/PlaceSlot — согласовать с тимлидом).
RISK: Нулевой — private object-ключи subcompose. Проверка: компиляция.

## [LOW] AdditionalTop.kt:36
PROBLEM: Параметры AdditionalTopStack: card и surface — «surface» коллидирует с Material Surface (а это ВЕСЬ лист целиком), «card» не связан со словарём слота additionalTop; в вызове (XBottomSheet.kt:408–409) card = additionalTop, surface = sheetSurface — маппинг имён приходится держать в голове.
FIX: card → additionalTopContent, surface → sheetContent.
RISK: Нулевой — rename параметров internal composable. Проверка: компиляция + кейс (g).

## [LOW] SheetSnapshotFlowManager.kt:24
PROBLEM: SheetSnapshotFlowManager — суффикс «Manager» размыт (менеджеров нет — это composable без UI, запускающий 3 snapshotFlow-коллектора), а «SnapshotFlow» — деталь реализации в имени. По Compose-гайду non-UI composable именуется глаголом (как LaunchedEffect).
FIX: Переименовать в ObserveSheetState(...) или SheetStateObservers — имя говорит «наблюдаем за стейтом», а не «управляем snapshotFlow».
RISK: Нулевой — rename internal composable + имя файла. Проверка: компиляция.

## [LOW] XBottomSheetState.kt:206
PROBLEM: Магический литерал 0.5f в условии входа в overshoot (offset.value >= anchor - 0.5f) — рядом в companion есть ANCHOR_EPS = 1f, но это ДРУГОЙ порог с другой семантикой, без имени и объяснения, почему именно 0.5.
FIX: Вынести в companion: const val OVERSHOOT_ENTER_EPS_PX = 0.5f с комментарием (допуск «палец у якоря» для старта rubber-band).
RISK: Нулевой — извлечение константы. Проверка: растяжение с сопротивлением в кейсе (a).

## [LOW] XBottomSheetState.kt:50
PROBLEM: rawOvershootPx — «raw» скрывает главное: это НАКОПЛЕННЫЙ пальцем overshoot (rawOvershootPx += delta в :208), к которому потом применяется кривая сопротивления. «Сырой vs сопротивлённый» понятно только после чтения dragBy.
FIX: Переименовать в accumulatedOvershootPx (пара с resistedOvershoot остаётся читаемой: накопили → применили сопротивление).
RISK: Нулевой — private поле. Проверка: компиляция.

## [LOW] Preset1Button.kt:14
PROBLEM: Preset1Button — цифра в середине имени читается как индекс («пресет №1»), а не «одна кнопка»; вне контекста Figma-слоя «x/1 Button» имя не самообъясняется.
FIX: Переименовать в PresetSingleButton, Figma-имя «x/1 Button» оставить в комментарии для трассировки к спеке.
RISK: Нулевой — rename internal composable + вызовы в демо. Проверка: компиляция.

## [LOW] PresetMenuCell.kt:53
PROBLEM: Неоднородный кейс Dp-токенов: в presets SCREAMING_SNAKE для не-const val (CELL_HEIGHT/MARKER_SIZE :53–54, BUTTON_HEIGHT в Preset1Button.kt:35), а в самом компоненте и PresetSpacing — PascalCase (ScrimFadeDistance, ShadowElevation, PresetSpacing.Horizontal). В демо тот же разнобой: SPORTS (SCREAMING, XbsDemoScreen.kt:92) против MarkerColors (PascalCase, :103) в одном файле.
FIX: Единый стиль: PascalCase для immutable val-токенов (Compose-конвенция: CellHeight, MarkerSize, ButtonHeight, Sports), SCREAMING только для настоящих const val (FLING_VELOCITY_THRESHOLD и т.п. уже корректны).
RISK: Нулевой — rename приватных констант. Проверка: компиляция.

OK: Комментарии однородно на русском во всех файлах компонента, пресетов и демо — стиль выдержан (verbatim-копия org/xplatform/uikit вне аудита, как и требовалось). | Magic numbers в основном уже вынесены: 400f/240f/1f/500L — именованные константы companion (FLING_VELOCITY_THRESHOLD, RESISTANCE_MAX_PX, ANCHOR_EPS, REMEASURE_TIMEOUT_MS); 120.dp = ScrimFadeDistance; 192.dp = LoadingSheetHeight; 2000L = LOADING_DELAY_MS в демо; 48/52/20.dp — константы в пресетах. Не вынесен только 0.5f (см. находку). | enqueueDrag/enqueueSettle — точные глаголы, честно описывают механику FIFO-канала (trySend в Channel); менять не нужно. | peekPx/peekFraction — «peek» это стандартный словарь bottom-sheet (peekHeight в Material BottomSheetBehavior), понятен любому Android-разработчику; оставить. | SheetValue и его значения (Hidden/Content/Collapsed/ExpandedContent/ExpandedFullScreen/Loading/Custom) — совпадают со словарём Material3 SheetValue и спекой §4; нейминг канонический. | Правило «именованные параметры лямбд, без it» соблюдено практически везде: candidate ->, anchor ->, entry ->, value ->, sport ->, remeasured -> — стиль однородный. | X-префикс семейства (XBottomSheet, XBottomSheetState, XSheetAnchor, XTheme, XBottomSheetDefaults) однороден и соответствует 1XUI-конвенции. | Файловая декомпозиция по ролям (SheetAnchors/SheetNestedScroll/SheetDragGestures/SheetSnapshotFlowManager/AdditionalTop/DragHandle) — логичная, каждый файл — один уровень абстракции; заголовочные комментарии файлов описывают


# Линза 2 — Память/CPU/GPU: аллокации в горячих путях (measure/draw/жесты), покадровая рекомпозиция, GPU-слои (clip/shadow), каналы/корутины, Saver

## [HIGH] XBottomSheet.kt:108
PROBLEM: additionalTopFraction через animateFloatAsState читается в теле XBottomSheet и пробрасывается Float-параметром вниз (SheetContainer → SheetBody → AdditionalTopStack). Каждый кадр spring-анимации (~300–500 мс) рекомпозирует ВЕСЬ лист, причём тело живёт в ДВУХ субкомпозициях SubcomposeLayout (detect + place) — двойная рекомпозиция за кадр. Каждый кадр: пересоздаются bodyOf/sheetSurface/middleContent-лямбды, SideEffect (строка 132) кладёт в state новую лямбду onDismissRequest, AdditionalTopStack аллоцирует новый AdditionalTopMeasurePolicy (AdditionalTop.kt:41 — remember с ключом visibleFraction) и новую RoundedCornerShape (AdditionalTop.kt:53), новая policy форсит remeasure. Прямое нарушение жёсткого правила: анимации не через animate*AsState.
FIX: Фракцию держать в Animatable: remember(state) { Animatable(если Expanded 1f иначе 0f) } + LaunchedEffect(state.additionalTopState) { fraction.animateTo(target, NativeSheetSpring) }. В AdditionalTopStack передавать сам Animatable (стабильный инстанс, без лямбды — нет проблемы stale-capture), MeasurePolicy запоминать один раз по remember(overlapPx), а fraction.value читать ВНУТРИ measure — snapshot-чтение в layout инвалидирует только layout-фазу, композиция за кадры не трогается. Из сигнатур SheetContainer/SheetBody убрать Float-параметр.
RISK: Анимация должна остаться плавной — гарантируется layout-инвалидацией по чтению Animatable.value в measure. Проверить demo-кейс (g): Expanded↔Collapsed, и что после restore (ротация/process-death) Animatable стартует с фракции, соответствующей восстановленному additionalTopState.

## [MEDIUM] XBottomSheet.kt:278
PROBLEM: Слот DETECT — полная вторая живая композиция всего контента листа (top+middle+bottom, включая LazyColumn с вьюпортом высотой в экран и OutlinedTextField): память на дерево узлов ×2, и каждая смена контент-стейта (ввод в поиск в кейсах i/l/s, рост списка) рекомпозирует и перемеряет контент дважды. Дополнительно measure-лямбда на каждом layout-пассе (каждый кадр драга) создаёт две новые лямбды { bodyOf(false) } / { bodyOf(true) }; из-за смены referencial identity контента SubcomposeLayout на каждый пасс перезапускает рекомпозицию слота (спасает только skip SheetBody по равным аргументам). Сам detect-замер при неизменных Constraints дешёвый (кэш LayoutNode) — основная цена именно память ×2 и двойная работа при изменениях контента.
FIX: Архитектура легализована юзером (intrinsics не работают с Lazy) — не переделывать. Два дешёвых шага: (1) вынести композейбл-лямбды детект/плейс контента из measure-лямбды в композицию (val detectContent / placeContent рядом с bodyOf, БЕЗ remember) — стабильная identity убирает и 2 аллокации на кадр, и холостой re-set контента слота каждый пасс; (2) зафиксировать в XBOTTOMSHEET.md ограничение «контент листа композируется дважды — держать middle лёгким».
RISK: Вынос лямбд — поведенчески нейтрален (проверить кейсы b1/b2/d: wrap/fill-детекция и Loading→contentReady). Отказ от detect-слота как таковой ломает wrap/fill-механику — только отдельным спайком.

## [MEDIUM] XBottomSheetState.kt:196
PROBLEM: При dismissOnSwipeDown=false каждый Drag из канала (≈ каждое move-событие пальца, 60–120 Гц) вызывает lowestRestAnchorPx → settleCandidates: mutableListOf + Pair<SheetValue, Int> с боксингом Int на каждый якорь + distinctBy (LinkedHashSet + новый список) + sortedBy (копия + сортировка) — порядка 10–15 аллокаций на каждое движение пальца, тысячи в секунду во время драга. GC-давление в самом горячем жестовом пути, при том что результат зависит только от metrics и не меняется в течение жеста.
FIX: Floor не зависит от offset — кэшировать: приватное поле lowestRestAnchorPxCache, пересчитываемое в updateMetrics при фактической смене metrics (или прямой min-перебор якорей по веткам when без списка и Pair). settle-путь (раз на отпускание) можно не трогать.
RISK: Не изменить семантику floor = самый нижний разрешённый якорь (фикс раунда 4). Регрессия demo-кейса (j): палец тянет вниз — лист стоит на якоре; (a)/(b1) с dismissOnSwipeDown=true — свайп по-прежнему закрывает.

## [MEDIUM] AdditionalTop.kt:81
PROBLEM: AdditionalTopMeasurePolicy.measure всегда возвращает totalHeight = constraints.maxHeight. В detect-пассе (maxHeight = ceiling) это даёт contentHeightPx == потолку → при заданном additionalTop лист ВСЕГДА в fill-режиме: wrap-стейты Content/ExpandedContent недостижимы, короткий контент открывается на peek 2/3 экрана. Для линзы перфа следствие: detect-замер с AdditionalTop не несёт информации, но выполняется каждый пасс; по существу это поведенческий вопрос (линза 1 — корректность) — здесь фиксирую механику как смежную находку.
FIX: Согласовать с линзой 1/тимлидом: в measure возвращать min(constraints.maxHeight, protrusion + натуральная высота surface-ребёнка) — по аналогии с собственным override maxIntrinsicHeight этой же policy. Тогда wrap-детекция заработает и с AdditionalTop.
RISK: Меняет текущее (возможно принятое) поведение кейса (g) — сейчас лист с AdditionalTop живёт в fill-режиме. Менять только осознанно, с прогоном (g) в обоих состояниях и проверкой якорей.

## [LOW] XBottomSheetState.kt:140
PROBLEM: updateMetrics вызывается из measure-лямбды SubcomposeLayout на КАЖДОМ layout-пассе (каждый кадр драга/settle/rubber-band): безусловно аллоцируется новый SheetMetrics + полный data-class equals (5 полей + список customAnchors) только чтобы выяснить, что ничего не изменилось. Небольшой объект, но это самый горячий путь компонента — мусор на каждый кадр на ровном месте.
FIX: Сравнение по полям ДО аллокации: val current = metrics; если current != null и screenHeightPx/statusBarPx/contentHeightPx/loadingSheetHeightPx совпадают — return (peekFraction и customAnchors — константы конструктора стейта, сравнивать не нужно); только при отличии создавать SheetMetrics и писать в state.
RISK: Нулевой при сравнении ровно тех же полей. Проверить кейс (k): рост контента по-прежнему триггерит snapshotFlow{contentHeightPx} → onContentRemeasured → авто-FullScreen.

## [LOW] AdditionalTop.kt:53
PROBLEM: clip(RoundedCornerShape(topStart, topEnd)) уже обрезает контент по границам бокса с формой; следующий за ним clipToBounds() — второй graphicsLayer/clip-узел: лишний слой и лишняя операция клипа на каждый кадр отрисовки (лист перерисовывается покадрово при драге и анимации фракции). Дополнительно RoundedCornerShape аллоцируется на каждую рекомпозицию (до фикса animateFloatAsState — на каждый кадр), а cardMeasurable.maxIntrinsicHeight(width) в measure гоняет intrinsic-обход поддерева карточки каждый пасс (карточка маленькая — терпимо).
FIX: Убрать clipToBounds() (обрезку низа карточки, уходящего под Surface на overlap, уже делает clip формы); shape вынести в remember(cornerRadius). Кэширование intrinsic-высоты карточки — опционально, только если карточки станут тяжёлыми.
RISK: Визуальная проверка кейса (g) в обоих состояниях: низ карточки по-прежнему обрезается, верхние углы скруглены, при частичной протрузии контент не вылезает за границы.

## [LOW] XbsDemoScreen.kt:467
PROBLEM: SPORTS.take(50).filter{...} выполняется прямо в композиции кейсов i/l/s: на каждой рекомпозиции скоупа (не только от query) — две аллокации списков + contains по 50 строкам. Demo-код, но образец для копирования в фичи.
FIX: val filtered = remember(query) { SPORTS.take(50).filter { sport -> sport.contains(query, ignoreCase = true) } } — derivedStateOf не нужен, ключ один и меняется дискретно.
RISK: Нет; проверить фильтрацию при вводе в кейсах (i)/(l)/(s).

## [LOW] XbsDemoScreen.kt:591
PROBLEM: AdditionalTopCard использует animateFloatAsState для contentAlpha — нарушение жёсткого правила «анимации не через animate*AsState». Механически чтение уже отложено в graphicsLayer-лямбду (кадровых рекомпозиций нет), но API из запрещённого списка и в demo служит анти-образцом.
FIX: remember { Animatable(...) } + LaunchedEffect(collapsed) { alpha.animateTo(if (collapsed) 0f else 1f, NativeSheetSpring) }; чтение alpha.value оставить внутри graphicsLayer — как сейчас.
RISK: Нет — визуально идентичный кросс-фейд; проверить кейс (g).

OK: offset — Animatable, единственный писатель — suspend-методы стейта; чтение offset.value отложено в measure-лямбду place-слота (XBottomSheet.kt:287) и в drawBehind скрима (XBottomSheet.kt:201) — драг/settle не вызывают ни одной рекомпозиции, только layout/draw-инвалидации. Канон defer reads. | Скрим: при overlayBackground=false лямбда drawBehind не читает offset (чтение под if) → нет даже draw-инвалидаций по кадрам; drawRect с готовым ScrimColor — без аллокаций в draw. | Жестовый канал: единственный потребитель processGestures на стейт (LaunchedEffect(state), XBottomSheet.kt:137), settle — дочерний Job с cancelAndJoin, coroutineScope хоронит всё при уходе из композиции — утечек Job нет. Channel(UNLIMITED) на практике не растёт: потребитель (быстрый snapTo) не блокируется, settle крутится сбоку и отменяется до первого кадра. Аллокация data-class команды на move-событие — осознанная цена сериализации из раунда 6 (фикс гонки drag↔settle); конфлейтить не стоит. | SheetNestedScrollConnection и DraggableState пересоздаются только событийно (remember(state, interactionsEnabled)); колбэки nested-scroll без heap-аллокаций (Offset/Velocity — value classes). | StayUnderKeyboardMeasurePolicy: Constraints.copy — inline value class, без heap-аллокаций в measure; override maxIntrinsicHeight защищает от Infinity-замера (пойманный краш раунда 5); policy пересоздаётся только при смене keyboardHeightPx/navBarPx — событийно, несколько раз на toggle IME. | rememberSaveable Saver: фабрика Saver и е


# Линза 3: бизнес-логика и корректность (стейт-машина, жесты, IME, restore, реактивные коллекторы)

## [HIGH] XBottomSheetState.kt:86
PROBLEM: contentReady() не проверяет isVisible: сценарий «show() в Loading → юзер закрыл лист тапом/hide() во время загрузки → приходит contentReady()» приводит к animateTo(openTarget) — закрытый лист САМ переоткрывается без действия юзера (currentValue уходит из Hidden, offset анимируется вверх, scrim возвращается). Все остальные внутренние реакции (onContentRemeasured, onImeShown) такой гард имеют, contentReady — нет. Дополнительно hide() может прийти ВО ВРЕМЯ 500мс-ожидания ре-замера внутри contentReady — итог тот же.
FIX: В начале: `if (!isVisible) { isLoading = false; return }`; и повторный `if (!isVisible) return` ПОСЛЕ ожидания метрик (перед animateTo) — hide() мог прилететь в окно ожидания. isLoading сбрасывать в обоих случаях, чтобы повторный show() открыл уже реальный контент.
RISK: Риск: сломать кейс (d) Loading→contentReady. Проверка: (d) обычный сценарий — Loader 192dp → анимация к цели; затем (d) с закрытием листа во время delay — лист НЕ должен переоткрыться; повторный show() после этого открывает готовый контент без Loader.

## [MEDIUM] AdditionalTop.kt:81
PROBLEM: AdditionalTopMeasurePolicy.measure берёт `totalHeight = constraints.maxHeight` и всегда возвращает layout(width, maxHeight). В detect-слоте SubcomposeLayout (XBottomSheet.kt:278, Constraints с loose maxHeight=ceilingPx) это даёт contentHeightPx == ceiling → fills == true ВСЕГДА, когда задан additionalTop-слот. Любой лист с additionalTop навсегда в fill-режиме: короткий контент открывается Collapsed на 2/3 экрана с пустотой вместо Content-по-контенту; wrap-стейты (Content/ExpandedContent) недостижимы. Demo-кейс (g) прошёл только потому, что там длинный список (fills и так true).
FIX: В measure различать пассы: если высота loose (constraints.minHeight < constraints.maxHeight — detect), мерить surface loose-констрейнтами (0..maxHeight−protrusion) и возвращать layout(width, protrusion + surfacePlaceable.height); для fixed-пасса (place) оставить текущее поведение как есть.
RISK: Риск: сломать вёрстку кейса (g) (перекрытие 32dp, анимация fraction) и place-пасс. Проверка: (g) в обоих состояниях Expanded/Collapsed + новый ручной кейс «additionalTop + короткий контент» — должен открыться Content по высоте контента; (a)/(b1) без additionalTop — регресс не задет.

## [MEDIUM] XBottomSheetState.kt:100
PROBLEM: Условие авто-FullScreen `measured.contentHeightPx > measured.maxHeightPx` — мёртвый код: detect-замер идёт при maxHeight=ceilingPx (XBottomSheet.kt:279), а Placeable.height коэрсится в констрейнты, т.е. contentHeightPx НИКОГДА не превышает maxHeightPx (максимум равен). При skipCollapsed+Content и переросшем контенте срабатывает else-ветка: offset уезжает к maxHeightPx (визуально полный экран), но currentValue ОСТАЁТСЯ Content, а не ExpandedFullScreen. Следствия: isFullScreen=false → неправильный гейтинг клавиатуры (withAdjustmentForKeyboard вместо withKeyboardShrink на листе у потолка), resistance считается по правилам Content (overshootCeiling без зоны статус-бара). Логи раунда (k) `content=2381 maxH=1766` относятся к СТАРОМУ intrinsic-измерителю — после перехода на SubcomposeLayout ветка недостижима. Аналогично мёртв `else -> ExpandedFullScreen` в expandTarget (SheetAnchors.kt:63).
FIX: Заменить условие на `measured.fills`: `if (skipCollapsed && currentValue == SheetValue.Content && measured.fills) animateTo(SheetValue.ExpandedFullScreen)`. fills (contentHeightPx >= maxHeightPx, SheetAnchors.kt:43) — единственный достижимый признак overflow при клампящем замере.
RISK: Риск: ложный FullScreen для контента ровно в потолок (но это и есть определение fills — поведение по спеке). Проверка: (k) рост контента при skipCollapsed → currentValue должен стать ExpandedFullScreen (лог/поведение IME-shrink), (c) короткий skipCollapsed-контент остаётся Content.

## [MEDIUM] SheetSnapshotFlowManager.kt:48
PROBLEM: snapshotFlow { currentValue == Hidden } эмитит НАЧАЛЬНОЕ значение при подписке. Лист, законно смонтированный в скрытом состоянии (компонент в композиции до show() — обычный паттерн), на первом же кадре получает hidden=true → onSheetHidden() → keyboardController.hide() + focusManager.clearFocus(force=true). Если на хост-экране в этот момент открыта клавиатура и фокус в поле (юзер печатает, а экран композирует лист заранее) — монтирование листа молча роняет юзеру клавиатуру и фокус.
FIX: Реагировать только на ПЕРЕХОД в Hidden, а не на начальное состояние: `snapshotFlow { state.isVisible }.drop(1).collect { visible -> if (!visible) onSheetHidden() }` (kotlinx.coroutines.flow.drop). Restore-путь не страдает: после process-death в Hidden ронять нечего.
RISK: Риск: пропустить дроп IME при закрытии (правило §6.5). Проверка: (i)/(l) — фокус в поиске, тап по скриму → клавиатура падает, лист закрывается прижатым к низу; плюс сценарий «лист в композиции, но скрыт, фокус в поле хоста» — клавиатура НЕ должна падать.

## [MEDIUM] SheetNestedScroll.kt:52
PROBLEM: onPreFling гейтится на interactionsEnabled: если флаг сменился МИД-ЖЕСТ (показ IME от программного focusRequest во время драга, isLoading=true, dragHandle=null от рекомпозиции) — remember(state, interactionsEnabled) в XBottomSheet.kt:139 подменяет connection на выключенный, settle НЕ энкьюится, а isDragging уже выставлен enqueueDrag'ом. isDragging залипает true → (а) offset замирает на промежуточной высоте (никто не доводит к якорю); (б) SheetScrim (XBottomSheet.kt:196) проверяет `!state.isDragging` → dismissOnOutsideTap перестаёт работать до следующего УСПЕШНОГО цикла drag+settle, а при dragHandle=null такого цикла больше не будет никогда — тап снаружи мёртв навсегда.
FIX: В onPreFling гейтить settle ТОЛЬКО по isDragging: `if (!state.isDragging) return Velocity.Zero; state.enqueueSettle(available.y)` — settle лишь завершает уже начатый драг (если жесты были выключены изначально, isDragging и так false). Дополнительно/альтернативно: при переходе interactionsEnabled true→false вызывать state.enqueueSettle(0f) — принудительное довершение висящего жеста.
RISK: Риск: settle подвинет лист при видимой IME (формально против правила §6.4 «жесты при IME выключены») — но это довершение начатого до IME жеста, лучше зависшего листа. Проверка: обычные (a)/(b1)/(b2) свайпы; (i) свайп при открытой IME по-прежнему двигает только список; сценарий «драг + программный фокус» — лист доводится к якорю, тап снаружи работает.

## [LOW] XBottomSheetState.kt:89
PROBLEM: Таймаут ре-замера 500мс в contentReady: если реальный контент меряется дольше (тяжёлая первая композиция LazyColumn) или композиция заморожена (приложение свернули — measure не идёт до foreground), фолбэк `?: awaitMetrics()` возвращает ТЕ ЖЕ stale-метрики Loader'а (metrics не менялся, reference совпадает с loaderMetrics). openTarget считается по высоте Loader'а (192dp+navBar ≤ peek) → всегда Content. Коллектор роста потом доведёт ВЫСОТУ (offset к min(content,peek)), но лейбл останется Content при контенте > peek: программный expand() станет no-op (работает только из Collapsed) до первого ручного жеста.
FIX: Фолбэк не должен принимать заведомо лоадерные метрики: после таймаута ждать метрики с contentHeightPx != loadingSheetHeightPx (или дождаться первого кадра после снятия isLoading через withFrameNanos и взять следующий инстанс метрик), либо после фолбэка пересчитывать currentValue в onContentRemeasured (если Content и !skipCollapsed и content > peekPx → animateTo(Collapsed)).
RISK: Риск: вечное ожидание при контенте, чья высота случайно равна Loader-якорю — оставить второй таймаут-предохранитель. Проверка: (d) обычный; (d) со сворачиванием приложения сразу после show() и разворотом после contentReady — лист в правильном стейте, expand() работает.

## [LOW] XBottomSheetState.kt:118
PROBLEM: onImeShown при Loading уходит в `else -> Unit` (осознанно), но top-слот с PresetSearchField виден и фокусабелен УЖЕ в Loading. Цепочка: фокус в поиске при Loading → IME видна, промоушена нет → contentReady() → animateTo(openTarget=Collapsed) при видимой клавиатуре. keyboardState не меняется → коллектор IME (SheetSnapshotFlowManager.kt:37, дедуп по значению) НЕ переэмитит → проверка liftedTop > maxHeightPx никогда не выполняется для нового стейта. withAdjustmentForKeyboard поднимает Collapsed-лист на высоту IME — верх листа уезжает под/за статус-бар, ровно то, от чего защищает авто-FullScreen.
FIX: Переоценивать промоушен при смене фактов под открытой клавиатурой: в SheetSnapshotFlowManager ключевать IME-коллектор парой `keyboardState.value to state.isLoading` (снятие Loading при видимой IME → повторный onImeShown, он идемпотентен). Порядок: пере-вызов после того, как contentReady выставил currentValue.
RISK: Риск: лишний вызов onImeShown при каждом флипе isLoading (безвреден — гард по стейтам внутри). Проверка: (i) обычный; новый сценарий «initialLoading + фокус в поиске во время Loading → contentReady» — лист должен авто-развернуться в FullScreen, а не уехать верхом за потолок.

## [LOW] XBottomSheetState.kt:213
PROBLEM: Не-overshoot ветка dragBy клампит вверх к measured.maxHeightPx для ЛЮБОГО стейта. Драг за хендл/top из Collapsed в wrap-режиме (expandTarget=ExpandedContent, контент < потолка) свободно тянет лист ВЫШЕ высоты контента до самого потолка без сопротивления: place-слот с fillHeight растягивает пустой Surface, на отпускании settle спружинит назад к ExpandedContent. Nested-scroll путь от этого защищён гардом expandAnchor (SheetNestedScroll.kt:24, фикс b1), а путь sheetDrag — нет; резистанс не включается, т.к. currentValue==Collapsed не входит в resistanceState.
FIX: Верхняя граница обычного драга = верхний rest-якорь (последний из settleCandidates(measured, dismissOnSwipeDown=false)), симметрично floorPx=lowestRestAnchorPx; выше якоря пускать только overshoot-ветку с сопротивлением (расширить resistanceState на случай «offset у верхнего якоря», а не только по currentValue).
RISK: Риск: задушить легитимный драг Collapsed→FullScreen в fill-режиме (там верхний якорь = maxHeightPx — граница совпадает, регресса нет) и rubber-band FullScreen (atTop-ветка не трогается). Проверка: (b1) хендл-драг вверх останавливается у ExpandedContent с сопротивлением дальше; (b2)/fill — драг до потолка как раньше.

## [LOW] XBottomSheetState.kt:206
PROBLEM: Вход в overshoot проверяется ПО-СОБЫТИЙНО: `offset.value >= anchor - 0.5f && delta > 0`. Быстрый драг вверх пересекает якорь одним событием (delta 50–100px/кадр): событие-пересечение ещё идёт по обычной ветке → offset = anchor + 99px БЕЗ сопротивления; следующее событие входит в overshoot с rawOvershootPx=0 → snapTo(anchor + resisted(delta)) ≈ anchor + 15px — видимый СКАЧОК ЛИСТА ВНИЗ на ~80px посреди жеста вверх.
FIX: Разрезать дельту на границе якоря: часть до anchor применять обычной веткой, остаток класть в rawOvershootPx тем же событием (offset = anchor + resistedOvershoot(остаток)). Одно событие — непрерывная траектория без отката.
RISK: Риск: арифметика разреза (знаки, coerce) — сломать медленный вход в overshoot. Проверка: медленный драг вверх в Content/FullScreen — сопротивление как раньше; резкий драг через якорь — монотонная траектория без скачка (видео/логи offset).

## [LOW] XBottomSheetState.kt:252
PROBLEM: Custom-якоря участвуют ТОЛЬКО в fill-ветке settleCandidates. Задокументировано («fill-режим, опц.»), но молча: разработчик задал customAnchors, контент оказался короче экрана (wrap) — якоря мертвы без какого-либо сигнала. Смежное: после process-death restore() честно восстанавливает currentValue=Custom(key) (Saver «cu:»), но если контент теперь wrap — Custom отсутствует в кандидатах, первый же settle уводит с него; anchorPx при этом работает (фолбэк на peekFraction при потерянном ключе), краша нет.
FIX: Минимум — `require`/лог в rememberXBottomSheetState о том, что customAnchors работают только в fill-режиме; либо включить их в wrap-ветки с клампом `coerceAtMost(anchorPx(expandTarget))` и distinctBy (дедуп уже есть на строке 269). Решение за team lead — это расширение контракта.
RISK: Риск (если включать в wrap): изменится набор остановок свайпа в кейсах (b1)/(r). Проверка: (r) кастомный якорь в fill; wrap-кейс с customAnchors — свайп останавливается на заклампленном якоре; restore в Custom при обоих режимах.

## [LOW] SheetNestedScroll.kt:54
PROBLEM: onPreFling при isDragging возвращает `available.copy(x = 0f)` — съедает ВСЮ вертикальную скорость. Составной жест «свайп вверх из Collapsed: лист дорос до expandAnchor, остаток жеста скроллил список» завершается тем, что инерция списка полностью гасится (isDragging остался true от фазы роста) — список останавливается мёртво вместо продолжения fling'а. Канонические реализации отдают списку неиспользованную settle'ом скорость.
FIX: Возвращать только реально потреблённую часть: если на момент preFling offset уже стоит на якоре (лист доехал до expandAnchor и дальше скроллил список), не съедать скорость — `if (abs(state.offset.value - ближайший якорь) < eps) { state.enqueueSettle(0f); return Velocity.Zero }`; иначе как сейчас.
RISK: Риск: двойное движение «лист доезжает + список флингует» может выглядеть неспокойно; настроить eps. Проверка: (b1)/(b2) один длинный свайп вверх — лист развернулся И список продолжил инерцию; обычный settle вниз/вверх не изменился.

OK: FIFO-канал жестов (Channel + единственный потребитель processGestures, cancelAndJoin settle-джоба) — гонка drag↔settle решена корректно, раунд 6 не трогать (XBottomSheetState.kt:173-188). | launch-в-launch в коллекторе роста (SheetSnapshotFlowManager.kt:34) джобы НЕ копит: каждый новый onContentRemeasured через мьютекс Animatable отменяет animateTo предыдущего, живы максимум 1-2 джоба; изоляция CancellationException в дочернем launch — правильная (без неё коллектор умирал, задокументировано). | distinctUntilChanged на KeyboardLiftState (SheetSnapshotFlowManager.kt:38) избыточен — snapshotFlow сам дедупит по equals, а KeyboardLiftState это data class (Boolean+Float) — но безвреден, можно не трогать. | restore после ротации/process-death работает: show() с гардом isVisible не сбивает восстановленный стейт, Saver Bundle-совместим (String/Boolean, тег cu:key для Custom), offset доводится snapToCurrentAnchor'ом (метрики к моменту эффекта уже посчитаны measure-пассом) либо коллектором роста при первом появлении метрик. | onImeShown корректно покрывает Custom-стейт (ветка в when + anchorPx→customAnchorPx с фолбэком на peekFraction при потерянном ключе); откат imePromotedFrom при скрытии IME и его сброс ручным взаимодействием (expand/settle) соответствуют §6 спеки. | Нижняя граница драга при dismissOnSwipeDown=false через lowestRestAnchorPx (фикс раунда 4) — корректна, включая fill-режим с кастомными якорями ниже peek. | StayUnderKeyboardMeasurePolicy: override maxIntrinsicHeight + g


# Линза 1: РЕКОМПОЗИЦИИ и фазы (composition → layout → draw), XBottomSheet + demo, Compose 1.11

## [HIGH] XBottomSheet.kt:108
PROBLEM: ЗАТРАВКА (б) ПОДТВЕРЖДЕНА, это худшая точка файла. `additionalTopFraction by animateFloatAsState(...)` читается в композиции корневого XBottomSheet (строка 168). На каждый КАДР анимации Expanded/Collapsed (~300мс x 60-120Hz): рекомпозируется весь XBottomSheet → SheetContainer (новый Float-параметр) → `bodyOf` обновляет захваты composableLambda, что инвалидирует ОБА slot-composition'а SubcomposeLayout (плюс SideEffect `forceRecomposeChildren` в SubcomposeLayout.kt:147 при нескипнутой рекомпозиции ставит forceRecompose обоим слотам) → SheetBody полностью рекомпозируется ДВАЖДЫ за кадр (detect+place), затем полный measure/layout. Прямое нарушение жёсткого правила юзера «никаких animate*AsState».
FIX: `val additionalTopFraction = remember(state) { Animatable(if (state.additionalTopState == AdditionalTopState.Expanded) 1f else 0f) }` + `LaunchedEffect(state) { snapshotFlow { state.additionalTopState }.collect { target -> additionalTopFraction.animateTo(if (target == Expanded) 1f else 0f) } }`. Вниз по дереву передавать НЕ Float, а сам Animatable (стабильная ссылка) до AdditionalTopStack; значение читать внутри MeasurePolicy.measure (см. фикс по AdditionalTop.kt). Тогда кадры анимации инвалидируют только layout этого Layout-узла: ноль рекомпозиций.
RISK: fraction влияет на замер detect-слота (contentHeightPx меняется во время анимации → onContentRemeasured подтягивает высоту листа) — при чтении в measure это поведение сохраняется, т.к. measure по-прежнему зависит от значения. Проверить кейс (g): плавное появление/полное скрытие карточки, конечная высота листа после анимации, восстановление после ротации (начальное значение Animatable из восстановленного additionalTopState).

## [HIGH] XBottomSheet.kt:100
PROBLEM: ЗАТРАВКА (а) ПОДТВЕРЖДЕНА по масштабу, ОПРОВЕРГНУТА по «покадровости». `keyboardState.value` (строки 100-101) читается в композиции корня. Источник — OnGlobalLayoutListener + getRootWindowInsets (KeyboardLift.kt:32-46): на API 30+ при SOFT_INPUT_ADJUST_NOTHING (его ставит withAdjustmentForKeyboard) инсеты диспатчатся целевым значением — это ДИСКРЕТНЫЕ события (показ/скрытие/смена высоты: emoji-панель, смена раскладки), а не кадры анимации IME. Но КАЖДОЕ такое событие рекомпозирует весь лист: imeVisible → interactionsEnabled (:104) → новый SheetNestedScrollConnection (:139); keyboardHeightPx → SheetContainer → bodyOf → двойная полная рекомпозиция SheetBody + каскад в liftBottomInset (:374) и StayUnderKeyboardMeasurePolicy (:435).
FIX: Убрать оба композиционных чтения. (1) `keyboardHeightPx` вообще не должен существовать как параметр: SheetBody уже получает `keyboardState: State<KeyboardLiftState>` — прокинуть его в StayUnderKeyboardContent и в модификатор нижнего инсета, читать `keyboardState.value` в measure-фазе (снапшот-чтение в measure инвалидирует только layout). (2) `interactionsEnabled` — `val interactionsEnabled by remember(state, dragHandle) { derivedStateOf { dragHandle != null && !state.isLoading && !keyboardState.value.isKeyboardVisible } }`: смена высоты клавиатуры при открытой IME больше не инвалидирует читателей, только реальные переключения булевой.
RISK: Риск — правило §6 «при видимой IME жесты выключены» и порядок показа bottom-кнопки. Проверить кейсы (i)/(l)/(s): авто-FullScreen, свайп при открытой клавиатуре не двигает лист, bottom под/над клавиатурой, возврат кнопки при скрытии IME; кейс (f) dragHandle=null и (d) Loading.

## [HIGH] AdditionalTop.kt:41
PROBLEM: ЗАТРАВКА (в) ПОДТВЕРЖДЕНА. `remember(visibleFraction, overlapPx) { AdditionalTopMeasurePolicy(...) }` — visibleFraction меняется каждый кадр анимации → каждый кадр: рекомпозиция AdditionalTopStack, аллокация нового MeasurePolicy, `set(measurePolicy)` на LayoutNode → полная инвалидация measure+layout стека (карточка intrinsic + два fixed-замера). Аллокация+композиция там, где нужен только layout-пасс.
FIX: Политика принимает `fraction: Animatable<Float, AnimationVector1D>` (или State<Float>) вместо Float: `remember(fraction, overlapPx) { AdditionalTopMeasurePolicy(fraction, overlapPx) }`, а `protrusionPx` читает `fraction.value` внутри measure/maxIntrinsicHeight. Animatable.value — снапшот-стейт: чтение в measure подписывает layout-узел, кадры анимации дают чистый remeasure без рекомпозиции и без аллокаций.
RISK: Механика протрузии не меняется (та же формула, тот же момент чтения относительно замера). Проверить (g): анимация обоих направлений, z-порядок (surface поверх карточки), корректность высоты в промежуточных кадрах при одновременном drag'е листа.

## [MEDIUM] XBottomSheet.kt:435
PROBLEM: ЗАТРАВКА (г) ПОДТВЕРЖДЕНА по механике, частота — не «каждый кадр IME», а каждое дискретное изменение keyboardHeightPx/navBarPx (см. находку по строке 100): `remember(keyboardHeightPx, navBarPx)` пересоздаёт StayUnderKeyboardMeasurePolicy, LayoutNode получает новую политику → полный remeasure. Плюс сам Int-параметр тащит рекомпозицию через всю цепочку XBottomSheet→SheetContainer→SheetBody→StayUnderKeyboardContent.
FIX: `StayUnderKeyboardMeasurePolicy(keyboardState: State<KeyboardLiftState>, navBarPx: Int)`, `remember(keyboardState, navBarPx) { ... }`; внутри measure: `val keyboardHeightPx = keyboardState.value.let { if (it.isKeyboardVisible) it.keyboardHeight.roundToInt() else 0 }`. Реакция на клавиатуру становится чисто layout-фазной: меняется только measure этого узла, композиция не трогается вовсе.
RISK: maxIntrinsicHeight использует только navBarPx — без изменений (гард от Infinity-краша из раунда 5 сохранить). Проверить (l): bottom уходит под клавиатуру и возвращается, замер contentHeight не ломается, нет краша intrinsic.

## [MEDIUM] XBottomSheet.kt:278
PROBLEM: ЗАТРАВКА (д) УТОЧНЕНА. Measure-лямбда читает offset.value (:287) → перезапускается каждый кадр drag/settle (это правильный deferred). Но `subcompose(SlotDetect) { bodyOf(false) }`: обёртка `{ bodyOf(false) }` создаётся в НЕ-композиционном контексте → новый composableLambdaInstance на каждый layout-пасс → в SubcomposeLayout.kt:642 `contentChanged = nodeState.content !== content` истинно КАЖДЫЙ кадр → ветка рекомпозиции обоих слотов каждый кадр (SheetBody скипается по равным аргументам, но снапшот-apply и обход групп — лишняя работа Recomposer'а на 60-120Hz). При этом страшилка «detect меряет ВСЁ тело каждый пасс» верна только когда слоты грязные: при стабильных constraints (width, ceilingPx) и чистом поддереве срабатывает кэш замера (MeasurePassDelegate.kt:485 — remeasure только при measurePending или смене constraints). Сегодня слоты грязные каждый кадр из-за находок по строкам 108/100 — вместе с ними detect-замер и правда полный покадровый.
FIX: Отдельный кэш detect-замера городить НЕ нужно — инвалидация «по contentHeight-инвариантам» уже обеспечена штатным constraints-кэшем. Достаточно: (1) убрать `bodyOf(Boolean)` и завести два значения в композиции SheetContainer: `val detectBody: @Composable () -> Unit = { SheetBody(..., fillHeight = false) }` и `val placeBody = ...` (без remember — composableLambda в композиции кэшируется слот-таблицей, идентичность стабильна между кадрами); передавать их в subcompose напрямую → contentChanged=false на каждом пассе; (2) закрыть находки 108/100, чтобы слоты не пачкались покадрово. После этого кадр drag'а = только measure place-слота (неизбежно: высота листа и есть анимируемая величина) + кэш-хит detect.
RISK: Детект wrap/fill обязан остаться на отдельной композиции — два разных val'а, не один общий. Проверить (a) wrap vs (b2) fill, (d) Loading→contentReady (переизмерение реального контента), рост контента (k), ротацию. Смок: скролл LazyColumn внутри place-слота не сбрасывается.

## [MEDIUM] XBottomSheet.kt:105
PROBLEM: `isFullScreen = state.currentValue == SheetValue.ExpandedFullScreen` — чтение currentValue в композиции корня. currentValue пишется в начале КАЖДОГО animateTo (show/expand/settle/drag-переходы), т.е. каждый переход стейта рекомпозирует весь лист (и через bodyOf — оба слота), хотя булева isFullScreen при большинстве переходов (Collapsed↔ExpandedContent, Content↔Hidden) не меняется.
FIX: `val isFullScreen by remember(state) { derivedStateOf { state.currentValue == SheetValue.ExpandedFullScreen } }` — инвалидация читателей только при пересечении границы FullScreen. Переключение модификаторов (withAdjustmentForKeyboard ↔ withKeyboardShrink) остаётся композиционным — это редкие дискретные события, допустимо.
RISK: Минимальный: семантика не меняется, только гранулярность инвалидации. Проверить (i): вход/выход из авто-FullScreen переключает adjustment→shrink (подъём против сжатия), и (b2) ручной разворот.

## [LOW] XBottomSheetState.kt:140
PROBLEM: updateMetrics вызывается из measure-лямбды SubcomposeLayout (XBottomSheet.kt:281) на каждом layout-пассе, т.е. на каждом кадре drag/settle аллоцирует SheetMetrics (data class + копия customAnchors-ссылки) только ради сравнения `if (updated != metrics)` — 60-120 короткоживущих объектов/сек в GC во время каждой анимации высоты.
FIX: Сравнивать примитивы до аллокации: `val current = metrics; if (current != null && current.screenHeightPx == screenHeightPx && current.statusBarPx == statusBarPx && current.contentHeightPx == contentHeightPx && current.loadingSheetHeightPx == loadingSheetHeightPx) return` и только потом создавать SheetMetrics. peekFraction/customAnchors — конструкторные, при живом стейте не меняются.
RISK: Практически нулевой: та же семантика равенства (peekFraction/customAnchors иммутабельны в рамках инстанса стейта). Проверить, что смена конфига через rememberSaveable-ключи (новый стейт) по-прежнему даёт свежие метрики.

## [LOW] XBottomSheet.kt:139
PROBLEM: `remember(state, interactionsEnabled) { SheetNestedScrollConnection(...) }` — на каждый флип interactionsEnabled (открытие/закрытие IME, конец Loading, смена dragHandle) аллоцируется новый connection и обновляется nestedScroll-модификатор. Не покадрово, но лишняя связка «булева в конструкторе» вместо чтения по месту.
FIX: Конструктор `SheetNestedScrollConnection(state, keyboardState, dragHandleVisible)`: внутри onPreScroll/onPostScroll/onPreFling вычислять доступность как `dragHandleVisible && !state.isLoading && !keyboardState.value.isKeyboardVisible` — колбэки жестов не являются снапшот-наблюдателями, чтение по месту ничего не инвалидирует; remember остаётся с ключами (state, dragHandle).
RISK: Проверить правило §6 п.4: при видимой IME nested-scroll двигает только список (кейс i), и (f)/(d) — жесты выключены. Поведение в середине жеста при флипе булевой станет «живым» (раньше фиксировалось на момент remember) — это соответствует ожиданию спеки.

## [LOW] XbsDemoScreen.kt:591
PROBLEM: `contentAlpha by animateFloatAsState(...)` в AdditionalTopCard — нарушение жёсткого правила юзера «никаких animate*AsState». Механика при этом почти правильная: чтение отложено в graphicsLayer{} (:599), т.е. кадры анимации идут в draw-фазе БЕЗ рекомпозиции — покадровой рекомпозиции тут нет, вопрос только соответствия правилу и единообразия с фиксом фракции листа.
FIX: `val contentAlpha = remember { Animatable(if (collapsed) 0f else 1f) }` + `LaunchedEffect(collapsed) { contentAlpha.animateTo(if (collapsed) 0f else 1f) }`; в graphicsLayer читать `alpha = contentAlpha.value` — фаза чтения остаётся draw.
RISK: Нулевой функциональный риск (demo). Проверить синхронность кросс-фейда текста с усадкой высоты в кейсе (g).

OK: ЗАТРАВКА (е) ПОДТВЕРЖДЕНА: SheetScrim.drawBehind (XBottomSheet.kt:199-204) читает state.offset.value в draw-фазе — канонический deferred read: alpha скрима обновляется каждый кадр анимации высоты чистой перерисовкой, без рекомпозиции и без relayout. Не трогать. | Чтение offset в measure-лямбде SubcomposeLayout (XBottomSheet.kt:287), а не в композиции — анимация высоты гоняет только layout-фазу. Правильная основа, на которую ложатся остальные фиксы. | offset как Animatable с единственным писателем (suspend-методы стейта) + FIFO-канал жестов (processGestures) — во время drag/settle нет покадровых записей в композиционный стейт; isDragging — mutableStateOf со structural equality, повторные записи true при драге не инвалидируют ничего. | updateMetrics защищён проверкой равенства (XBottomSheetState.kt:148) — metrics-стейт не дёргается на каждом пассе, snapshotFlow роста контента получает только реальные изменения. | Зависимость KeyboardAdjustmentNode (xbet-копия) двигает контент через placeWithLayer { translationY } + invalidatePlacement — подъём листа над IME идёт placement-фазой без рекомпозиций; проблема листа не в модификаторе, а в собственных композиционных чтениях keyboardState (см. находки). | Тело листа композируется по одному разу на слот (detect/place) — двойной ЗАМЕР, а не двойная жизнь контента в рантайме place-слота; сама схема wrap/fill-детекта звуковая, чинить нужно только идентичность content-лямбд и чистоту слотов. | Демо-карточка AdditionalTopCard читает alpha вн
